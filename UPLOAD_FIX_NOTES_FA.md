# اصلاح آپلود فایل — Portfolio

این نسخه سه مشکل مهم آپلود را اصلاح می‌کند:

1. محدودیت پیش‌فرض 1MB در Nginx به 12MB افزایش یافته است (Django همچنان سقف 10MB دارد).
2. Host و Port مرورگر به Django پاس داده می‌شود تا URL فایل آپلودشده روی `localhost:3000` و دامنه واقعی درست ساخته شود.
3. پنل React خطاهای 413، انقضای Login و فایل بزرگ‌تر از 10MB را با پیام واضح نمایش می‌دهد.

## اعمال روی پروژه فعلی

اگر نمی‌خواهی کل پروژه را جایگزین کنی، فقط این دو فایل را از نسخه اصلاح‌شده جایگزین کن:

- `frontend/nginx.conf`
- `frontend/src/components/UploadInput.jsx`

سپس:

```powershell
docker compose up -d --build frontend
```

بعد صفحه را با `Ctrl + F5` رفرش کن و دوباره عکس را آپلود کن.
