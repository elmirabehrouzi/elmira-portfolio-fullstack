# اصلاح ترجمه و دانلود رزومه

## چه چیزهایی اصلاح شد؟
- ترجمه کامل متن‌های ثابت صفحات عمومی برای EN / DE / FA.
- ترجمه Header، Footer، Side identity، Home، Projects، Project Detail، About، Skills، Experience، Design، Lab/Blog، Contact و Resume.
- ترجمه دسته‌بندی پروژه‌ها و مهارت‌ها.
- تاریخ وبلاگ بر اساس زبان انتخابی نمایش داده می‌شود.
- Location در Footer بر اساس زبان انتخابی خوانده می‌شود.
- نقش و متن معرفی در Header/Side rail از پروفایل سه‌زبانه خوانده می‌شود.
- دکمه دانلود رزومه صفحه Home فقط فایل همان زبان فعال را نمایش می‌دهد.
- صفحه Resume سه فایل مستقل EN / DE / FA را نمایش می‌دهد و برای هرکدام دانلود PDF دارد.
- Preview رزومه از role و bio همان زبان فعال استفاده می‌کند.

## رزومه چه زمانی قابل دانلود است؟
1. وارد `/control/profile` شوید.
2. برای Resume EN PDF، Resume DE PDF و Resume FA PDF فایل PDF آپلود کنید.
3. بعد از پایان آپلود حتماً روی `Save profile` بزنید.
4. فایل‌ها در media ذخیره می‌شوند و URL آن‌ها در SiteProfile ثبت می‌شود.
5. پس از آن صفحه `/resume` لینک دانلود هر سه نسخه را فعال می‌کند.
6. در Home فقط رزومه متناظر با زبان فعلی نمایش داده می‌شود.

## نکته مهم درباره ترجمه محتوا
رابط سایت اکنون ترجمه می‌شود، اما متن‌هایی که خودتان از پنل وارد می‌کنید ترجمه ماشینی نمی‌شوند. برای Project / Blog / Bio / Experience و مواردی که فیلد English / فارسی / Deutsch دارند، باید نسخه هر زبان را در فیلد خودش وارد کنید. اگر فیلد فارسی یا آلمانی خالی باشد، سایت به نسخه انگلیسی fallback می‌کند.

## اعمال نسخه جدید
پس از جایگزین کردن فایل‌های پروژه:

```powershell
docker compose up -d --build frontend
```

سپس در مرورگر:

`Ctrl + F5`
