import React,{useState} from 'react'
import {useLocation,useNavigate} from 'react-router-dom'
import {LockKeyhole,UserRound,ArrowRight} from 'lucide-react'
import api from '../../api'
export default function Login(){const [username,setUsername]=useState('elmira');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const nav=useNavigate();const loc=useLocation();const submit=async e=>{e.preventDefault();setLoading(true);setError('');try{const {data}=await api.post('/auth/token/',{username,password});localStorage.setItem('eb_access',data.access);localStorage.setItem('eb_refresh',data.refresh);nav(loc.state?.from?.pathname||'/control')}catch{setError('Username or password is incorrect.')}finally{setLoading(false)}};return <div className="admin-login-page"><div className="login-art"><div className="login-monogram">EB.</div><h1>Build.<br/>Manage.<br/><span>Grow.</span></h1><p>Your private control room for projects, messages, writing and the portfolio itself.</p><div className="code-line">{`{ portfolio: "always evolving" }`}</div></div><form className="login-card" onSubmit={submit}><div className="eyebrow">PRIVATE ACCESS</div><h2>Control Panel</h2><p>Manage your live portfolio content.</p><label><span><UserRound size={16}/> Username</span><input value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username"/></label><label><span><LockKeyhole size={16}/> Password</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></label>{error&&<div className="form-error">{error}</div>}<button className="btn primary" disabled={loading}>{loading?'Signing in…':'Sign in'} <ArrowRight size={16}/></button><small>Change the initial password after first deployment.</small></form></div>}
